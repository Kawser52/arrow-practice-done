const practiceConst= 'Const kaj kore ki';
let practiceLet = 'Let dia kaj korle abar kaj kora jai ba assign kora jai';

const product = {
    name : 'apple',
    price : 30,
    color : 'red'
}

const templateString = `kire kaj kre kina dekh. ${practiceConst} and apple er kaj korchi ${product.price}`
const appleWork = `Name ${product.name} and Price is  ${product.price}`;
console.log(appleWork);