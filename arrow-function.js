const arrowFunction = num => num/5;
const value = arrowFunction (5);
console.log(value);

const multPly = (x,y)=>{
    const abul = x + 2;
    const jabul = y + 2;
    const multiPlication = abul * jabul; 
    return multiPlication;
}

const total = multPly(5, 4 );
console.log(total);

const simplateArrow = (num1, num2, num3)=> num1*num2*num3;
const mul = simplateArrow(2, 2 , 2);
console.log(mul);